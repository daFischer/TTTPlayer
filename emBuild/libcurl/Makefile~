
EM=em++ -02 -s ALLOW_MEMORY_GROWTH=1
AR=emar
RANLIB=ranlib

libcurl.bc: $(OBJS)
	$(CC) $(OBJS) -s EXPORT_ALL=1 -o $@

match.o: match.S
	$(EM) match.S > _match.s
	$(EM) -c _match.s
	mv _match.o match.o
	rm -f _match.s

OBJS = $(wildcard *.c)

